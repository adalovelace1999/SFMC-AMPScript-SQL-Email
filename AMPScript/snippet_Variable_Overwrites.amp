/*

%%[

// [CUSTOM PLAN NAME FUNCTION] 

IF NOT EMPTY(@PLAN_NAME_OVERWRITE)
THEN set @PLAN_NAME = @PLAN_NAME_OVERWRITE
ENDIF

// [CUSTOM COMPANY NAME FUNCTION]

IF NOT EMPTY(@COMPANY_NAME_OVERWRITE)
THEN set @COMPANY_NAME = @COMPANY_NAME_OVERWRITE
ENDIF

// [PHONE OVERWRITE] 

IF NOT EMPTY(@PHONE_NUMBER_OVERWRITE)
THEN set @PHONE_NUMBER = @PHONE_NUMBER_OVERWRITE
ENDIF

// [AGE OVERWRITE FUNCTION]

IF NOT EMPTY(@AGE_OVERWRITE)
THEN set @AGE = @AGE_OVERWRITE
ENDIF

]%%

  */